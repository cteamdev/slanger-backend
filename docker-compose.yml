version: '3.7'

services:
  postgres:
    image: postgres:latest
    restart: always
    env_file:
      - .env
    ports:
      - 5432:5432
    volumes:
      - postgres:/var/lib/postgresql

  redis:
    image: redis:latest
    restart: always
    env_file:
      - .env
    ports:
      - 6379:6379
    volumes:
      - redis:/data

  meilisearch:
    image: getmeili/meilisearch:latest
    env_file:
      - .env
    ports:
      - 7700:7700
    volumes:
      - meilisearch:/data.ms

  slanger-backend:
    build: ./
    restart: always
    env_file:
      - .env
    depends_on:
      - postgres
      - redis
      - meilisearch
    ports:
      - 3000:3000

  nginx:
    image: nginx:latest
    ports:
      - 80:80
      - 443:443
    volumes:
      - $PWD/nginx:/etc/nginx

volumes:
  postgres:
  redis:
  meilisearch:
  nginx:
